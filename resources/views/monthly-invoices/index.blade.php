@extends('layouts.app')
@section('content')
@php($money = app(App\Services\MoneyFormatter::class))
<div class="flex items-center justify-between"><h1 class="text-3xl font-extrabold text-villeneuve-forest">Monthly Invoices</h1><a class="btn btn-primary" href="{{ route('monthly-invoices.create') }}">New Invoice</a></div>
<form class="mt-6 flex flex-wrap gap-3" method="get"><input name="invoice_number" placeholder="Invoice #" value="{{ request('invoice_number') }}"><select name="client_id"><option value="">All clients</option>@foreach($clients as $client)<option value="{{ $client->id }}" @selected(request('client_id')==$client->id)>{{ $client->name }}</option>@endforeach</select><select name="status"><option value="">All statuses</option>@foreach(['draft','approved','sent','paid','cancelled'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><button class="btn btn-secondary">Filter</button></form>
<div class="panel mt-6 overflow-x-auto"><table class="table w-full"><tr><th>Invoice</th><th>Client</th><th>Month</th><th>Status</th><th class="text-right">Grand Total</th></tr>@foreach($invoices as $invoice)<tr><td><a class="font-bold text-villeneuve-green" href="{{ route('monthly-invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a></td><td>{{ $invoice->client->name }}</td><td>{{ $invoice->invoice_month }}/{{ $invoice->invoice_year }}</td><td>{{ $invoice->status }}</td><td class="text-right">{{ $money->format($invoice->grand_total_cents, $invoice->client->default_language) }}</td></tr>@endforeach</table></div>{{ $invoices->links() }}
@endsection
